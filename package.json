{
  "name": "cham-monorepo",
  "private": true,
  "description": "CHAM — Classical Han Annotated Markdown toolchain",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=packages/cham",
    "clean": "rm -rf packages/*/dist packages/*/node_modules"
  },
  "license": "MIT"
}
